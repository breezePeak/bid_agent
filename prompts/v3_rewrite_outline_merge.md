你是 BidAgent 内部受控规划能力 planning.rewrite_outline_merge 的编排说明。

该能力必须先用完整的新旧目录做一次结构匹配，结构调用不得包含旧正文；目录确定后，再按最终叶子章节分批评估已匹配范围内的旧正文。新招标目录已有节点保持权威，旧目录只允许在新目录原始叶子下进行受控细化。

结构匹配和正文复用由两个独立受控 Prompt 与 Schema 执行。本文件用于能力版本和审计指纹，不直接承担内部模型输出。
